import CreserRegistros from '@/components/rrhh/creser/CreserRegistros';

export const metadata = {
  title: 'CRESER — Reportes',
  description: 'Reportes y estadísticas CRESER por área y periodo',
};

export default function CreserRegistrosPage() {
  return <CreserRegistros />;
}
